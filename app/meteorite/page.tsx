import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Meteorite",
};

export default async function Meteorite() {
    return (
        <>
            <h1>Метеоритик</h1>
        </>
    );
}
